
sudo: required
services:
  -docker

before_install:
  -docker build -t AndresYE/docker-react -f Dockerfile.dev .
  script:
  - docker run -e CI=true AndresYE/docker-react npm run test -- --coverage
language: generic 